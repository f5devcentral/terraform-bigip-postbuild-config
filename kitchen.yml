---
driver:
  name: terraform
  root_module_directory: test/fixtures/wrapper
  parallelism: 4
  command_timeout: 1200

provisioner:
  name: terraform

verifier:
  name: terraform

platforms:
  - name: bigip
    
    driver:
      variable_files:
        - test/assets/bigip.tfvars
    verifier:
      systems:
        - name: local
          backend: local
          controls:
            - bigip-connectivity



suites:
  - name: bigip